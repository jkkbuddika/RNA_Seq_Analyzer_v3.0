class UserDefinedOptions:

    ## Model organism
    species = input('Enter the species code (Options: hs, mm, dm, ce, dr or custom): ')
    runMode = input('Enter the Run Mode (Options: 0 for single-end or 1 for paired-end): ')
    dedup = input('Deduplication (Options: TRUE or FALSE): ')
